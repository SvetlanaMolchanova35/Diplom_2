[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --tb=short -v --alluredir=allure-results
markers =
    smoke: smoke tests
    api: api tests
    user: tests for user creation
    login: tests for user login
    order: tests for order creation